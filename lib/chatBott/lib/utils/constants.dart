import 'package:flutter/material.dart';

// Colors used in this app
const secondaryColor = Color.fromRGBO(210, 207, 251, 1);

const primaryColor = Color.fromRGBO(210, 207, 251, 1);

// Default App Padding
const appPadding = 16.0;
